{
  "name": "presidents-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node --env-file=.env scripts/dev-server.mjs",
    "db:setup": "node --env-file=.env scripts/setup-db.mjs",
    "db:setup:prod": "node --env-file=.env.production scripts/setup-db.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@simplewebauthn/browser": "^13.2.2",
    "@simplewebauthn/server": "^13.2.2",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  },
  "engines": {
    "node": ">=22"
  }
}
